{
  "name": "poster-studio-pro",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx --yes serve public -l 5173",
    "build": "node scripts/build-static.mjs",
    "preview": "npx --yes serve dist -l 4173",
    "deploy:apptesting": "npm run build && python .github/scripts/deploy-apptesting.py"
  }
}
